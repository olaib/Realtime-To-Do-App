import { CreateConnectionDto } from './create-connection.dto';

describe('CreateConnectionDto', () => {
  it('should be defined', () => {
    expect(new CreateConnectionDto()).toBeDefined();
  });
});
